minWidth = mean(widths) - 0.64*sd(widths)
peakData = IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
length(peakData$x)
smooth_z = myDataata$smooth_gyro_z
myData = read.csv("WALKING_AND_TURNING.csv", header=TRUE)
smooth_z = myData$smooth_accel_z
rawPeaks = IDPmisc::peaks(smooth_z)
widths = rawPeaks$w
minWidth = mean(widths) - 0.64*sd(widths)
peakData = IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
length(peakData$x)
smooth_z = myData$smooth_gyro_z
rawPeaks = IDPmisc::peaks(smooth_z)
peakData = IDPmisc::peaks(smooth_z,minPH = 2, thr = mean(smooth_z))
length(peakData$x)
myData = read.csv("TURNING.csv", header=TRUE)
smooth_z = myData$smooth_gyro_z
rawPeaks = IDPmisc::peaks(smooth_z)
peakData = IDPmisc::peaks(smooth_z,minPH = 2,thr = mean(smooth_z))
length(peakData$x)
myData = read.csv("WALKING.csv", header=TRUE)
smooth_z = myData$smooth_accel_z
rawPeaks = IDPmisc::peaks(smooth_z)
widths = rawPeaks$w
minWidth = mean(widths) - 0.64*sd(widths)
peakData = IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
length(peakData$x)
myData = read.csv("WALKING.csv", header=TRUE)
smooth_z = myData$smooth_accel_z
rawPeaks = IDPmisc::peaks(smooth_z)
widths = rawPeaks$w
minWidth = mean(widths) - 0.64*sd(widths)
peakData = IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
print(length(peakData$x))
myData = read.csv("TURNING.csv", header=TRUE)
smooth_z = myData$smooth_gyro_z
rawPeaks = IDPmisc::peaks(smooth_z)
peakData = IDPmisc::peaks(smooth_z,minPH = 2,thr = mean(smooth_z))
print(length(peakData$x))
myData = read.csv("WALKING_AND_TURNING.csv", header=TRUE)
smooth_z = myData$smooth_accel_z
rawPeaks = IDPmisc::peaks(smooth_z)
widths = rawPeaks$w
minWidth = mean(widths) - 0.64*sd(widths)
peakData = IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
print(length(peakData$x))
smooth_z = myData$smooth_gyro_z
rawPeaks = IDPmisc::peaks(smooth_z)
peakData = IDPmisc::peaks(smooth_z,minPH = 2, thr = mean(smooth_z))
print(length(peakData$x))
myData = read.csv("WALKING.csv", header=T)
smooth_z = myData$smooth_accel_z
rawPeaks = IDPmisc::peaks(smooth_z)
widths = rawPeaks$w
minWidth = mean(widths) - 0.64*sd(widths)
peakData = IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
print(length(peakData$x))
myData = read.csv("WALKING_AND_TURNING.csv", header=T)
smooth_z = myData$smooth_accel_z
rawPeaks = IDPmisc::peaks(smooth_z)
widths = rawPeaks$w
minWidth = mean(widths) - 0.64*sd(widths)
peakData = IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
print(length(peakData$x))
smooth_z = myData$smooth_gyro_z
rawPeaks = IDPmisc::peaks(smooth_z)
peakData = IDPmisc::peaks(smooth_z,minPH = 2, thr = mean(smooth_z))
print(length(peakData$x))
myData = read.csv("TURNING.csv", header=T)
smooth_z = myData$smooth_gyro_z
rawPeaks = IDPmisc::peaks(smooth_z)
peakData = IDPmisc::peaks(smooth_z,minPH = 2,thr = mean(smooth_z))
print(length(peakData$x))
myData <- read.csv("WALKING.csv", header=T)
smooth_z = myData$smooth_accel_z
rawPeaks = IDPmisc::peaks(smooth_z)
widths = rawPeaks$w
minWidth = mean(widths) - 0.64*sd(widths)
peakData = IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
print(length(peakData$x))
myData <- read.csv("WALKING.csv", header=T)
smooth_z <- myData$smooth_accel_z
rawPeaks <- IDPmisc::peaks(smooth_z)
widths <- rawPeaks$w
minWidth <- mean(widths) - 0.64*sd(widths)
peakData <- IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
print(length(peakData$x))
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
smooth_z <- myData$smooth_accel_z
rawPeaks <- IDPmisc::peaks(smooth_z)
widths <- rawPeaks$w
minWidth <- mean(widths) - 0.64*sd(widths)
peakData <- IDPmisc::peaks(smooth_z,minPW = minWidth,thr = mean(smooth_z))
print(length(peakData$x))
smooth_z <- myData$smooth_gyro_z
rawPeaks <- IDPmisc::peaks(smooth_z)
peakData <- IDPmisc::peaks(smooth_z,minPH = 2, thr = mean(smooth_z))
print(length(peakData$x))
myData <- read.csv("TURNING.csv", header=T)
smooth_z <- myData$smooth_gyro_z
rawPeaks <- IDPmisc::peaks(smooth_z)
peakData <- IDPmisc::peaks(smooth_z,minPH = 2,thr = mean(smooth_z))
print(length(peakData$x))
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
rawPeaks <- IDPmisc::peaks(accel_z)
widths <- rawPeaks$w
minWidth <- mean(widths) - 0.64*sd(widths)
peakData <- IDPmisc::peaks(accel_z,minPW = minWidth,thr = mean(accel_z))
print(length(peakData$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
rawPeaks <- IDPmisc::peaks(gyro_z)
peakData <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
print(length(peakData$x))
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
rawPeaks <- IDPmisc::peaks(accel_z)
widths <- rawPeaks$w
minWidth <- mean(widths) - 0.64*sd(widths)
peakData <- IDPmisc::peaks(accel_z,minPW = minWidth,thr = mean(accel_z))
print(length(peakData$x))
gyro_z <- myData$smooth_gyro_z
rawPeaks <- IDPmisc::peaks(gyro_z)
peakData <- IDPmisc::peaks(gyro_z,minPH = 2, thr = mean(gyro_z))
print(length(peakData$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
rawPeaks <- IDPmisc::peaks(gyro_z)
peakData <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
print(length(peakData$x))
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
widthVal <- peaks$w
minWidth <- mean(widthVal) - 0.64*sd(widthVal)
steps <- IDPmisc::peaks(accel_z,minPW = minWidth,thr = mean(accel_z))
print(length(steps$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
peakData <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
print(length(peakData$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
turns <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
print(length(turns$x))
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
widthVal <- peaks$w
minWidth <- mean(widthVal) - 0.64*sd(widthVal)
steps <- IDPmisc::peaks(accel_z,minPW = minWidth,thr = mean(accel_z))
print(length(steps$x))
gyro_z <- myData$smooth_gyro_z
turns <- IDPmisc::peaks(gyro_z,minPH = 2, thr = mean(gyro_z))
print(length(turns$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
turns <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
turns <- turns + IDPmisc::peaks(-gyro_z,minPH = 2,thr = mean(gyro_z))
print(length(turns$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
turns <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
count = turns
turns <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = mean(gyro_z))
count = count + turns
print(length(turns$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
peaks <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
troughs <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = mean(gyro_z))
total = peaks + troughs
print(length(total$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
peaks <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
troughs <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = mean(gyro_z))
total = length(peaks$x) + length(troughs$x)
print(total)
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
widthVal <- peaks$w
minWidth <- mean(widthVal) - 0.64*sd(widthVal)
steps <- IDPmisc::peaks(accel_z,minPW = minWidth,thr = mean(accel_z))
print(length(steps$x))
gyro_z <- myData$smooth_gyro_z
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = mean(gyro_z))
total = length(clockwise$x) + length(counter_clockwise$x)
print(total)
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = mean(gyro_z))
total = length(clockwise$x) + length(counter_clockwise$x)
print(total)
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minWidth <- mu - 0.64*SD
steps <- IDPmisc::peaks(accel_z,minPW = minWidth,thr = mean(accel_z))
print(length(steps$x))
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print(length(steps$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = mean(gyro_z))
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = mean(gyro_z))
total = length(clockwise$x) + length(counter_clockwise$x)
print(total)
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
print(total)
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print(length(steps$x))
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
print(total)
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print(length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
print(total)
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print(length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
print(turns: total)
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print(length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
print(turns: + total)
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print(length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
print(total)
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print("Steps: " + length(steps$x))
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print("Steps: ", length(steps$x))
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print(cat("Steps: ", length(steps$x)))
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print(cat("Steps:", length(steps$x)))
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print("Steps:", {length(steps$x)})
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print("Steps:", length(steps$x))
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
print("Steps:", length(steps$x))
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
sprintf("Steps: %i", length(steps$x))
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
sprintf("Steps: %i", length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
sprintf("Turns: %i" + total)
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
sprintf("Steps: %i", length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
sprintf("Turns: %i", total)
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
sprintf("Turns: %i", total)
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
sprintf("Steps: %i", length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
sprintf("Turns: %i", total)
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
sprintf("Steps: %i", length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
sprintf("Turns: %i", total)
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
sprintf("Steps: %i", length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
sprintf("Turns: %i", total)
knitr::opts_chunk$set(echo = TRUE)
myData <- read.csv("WALKING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
sprintf("Steps: %i", length(steps$x))
myData <- read.csv("WALKING_AND_TURNING.csv", header=T)
accel_z <- myData$smooth_accel_z
peaks <- IDPmisc::peaks(accel_z)
peakWidths <- peaks$w
mu <- mean(peakWidths)
SD <- sd(peakWidths)
minPw <- mu - 0.64*SD
threshold = mean(accel_z)
steps <- IDPmisc::peaks(accel_z,minPW = minPw,thr = threshold)
sprintf("Steps: %i", length(steps$x))
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
sprintf("Turns: %i", total)
myData <- read.csv("TURNING.csv", header=T)
gyro_z <- myData$smooth_gyro_z
threshold = mean(gyro_z)
clockwise <- IDPmisc::peaks(gyro_z,minPH = 2,thr = threshold)
counter_clockwise <- IDPmisc::peaks(-gyro_z,minPH = 2,thr = threshold)
total = length(clockwise$x) + length(counter_clockwise$x)
sprintf("Turns: %i", total)
